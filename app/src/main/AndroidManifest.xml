<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sylabs.evervote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EverVote">
        <activity android:name=".Donation"></activity>
        <activity android:name=".Feedback" />
        <activity android:name=".Complaint" />
        <activity android:name=".CandidateProfile" />
        <activity android:name=".Profile" />
        <activity android:name=".CandidatePost" />
        <activity android:name=".SearchCandidate" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>